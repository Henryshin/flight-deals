# 등록 프록시 설정 (Cloudflare Worker)

방문자가 GitHub 토큰 없이 노선을 등록/삭제할 수 있게 해주는 무료 서버(Worker)입니다.
토큰은 이 Worker 안에만 비밀로 보관되고, 페이지는 이 Worker를 통해서만 GitHub에 씁니다.

## 준비물
- 무료 Cloudflare 계정 (https://dash.cloudflare.com/sign-up)
- GitHub Fine-grained 토큰 1개 — Repository: `Henryshin/flight-deals`,
  권한: **Contents: Read and write**, **Actions: Read and write**
- 친구들에게 알려줄 **공유 암호**(아무 문자열) — 스팸 방지용 (원치 않으면 생략 가능)

## 단계 (약 10분, 코딩 없음)

1. Cloudflare 대시보드 → **Workers & Pages** → **Create** → **Create Worker**.
   이름은 예: `flight-register` → **Deploy**.

2. **Edit code** 클릭 → 기본 코드를 전부 지우고, 이 폴더의 `worker.js` 내용을
   **통째로 붙여넣기** → **Deploy**.

3. Worker의 **Settings → Variables and Secrets** 에서 아래를 추가:
   | 이름 | 종류 | 값 |
   |------|------|-----|
   | `GH_TOKEN` | Secret | 위에서 만든 GitHub 토큰 |
   | `SHARE_PASS` | Secret | 공유 암호 (생략 시 암호 없이 누구나 등록) |
   | `ALLOW_ORIGIN` | Text | `https://henryshin.github.io` |
   추가 후 **Deploy** 한 번 더.

4. 배포된 Worker 주소를 복사합니다. 형태: `https://flight-register.<계정>.workers.dev`

5. 그 주소를 알려주시면, 제가 `docs/index.html`의 `PROXY_URL`에 넣어 연결합니다.
   그 순간부터 방문자는 토큰 입력창 대신 **공유 암호만**(설정했다면) 입력하면 바로 등록됩니다.

## 동작
- 등록/삭제/수집이 이 Worker를 거쳐 실행됩니다.
- 서버에서 공항 코드·중복·id를 다시 검증하므로 클라이언트 조작에 안전합니다.
- 동시 등록으로 인한 저장 충돌은 자동 재시도로 처리합니다.
- 등록 즉시 해당 노선만 1회 수집을 트리거합니다.

## 주의
- 공용 `routes.json` 하나를 모두가 공유합니다 → 소규모(친구·소모임)용입니다.
  대규모 공개에는 맞지 않습니다(수집이 구글 화면 크롤링이라 노선이 많아지면 한계).
- 토큰이 노출되면(예: 실수로 커밋) 즉시 GitHub에서 폐기하고 새로 발급하세요.

## 토큰 만료 — "암호는 맞는데 등록/수정/삭제만 안 될 때"

GitHub Fine-grained 토큰은 **만료 기한이 있습니다.** `GH_TOKEN` 이 만료되면 GitHub 이
401을 돌려주므로, 공유 암호는 멀쩡한데 등록·수정·삭제만 실패합니다.

대시보드의 **공유 암호 창 → `암호 확인`** 버튼이 이 둘을 구분해 줍니다.

| 결과 | 뜻 | 조치 |
|------|-----|------|
| `암호 확인 ✓ 서버와 일치합니다 · GitHub 토큰도 정상` | 정상 | — |
| `암호가 서버와 다릅니다 (N자)` | 브라우저에 저장된 암호가 `SHARE_PASS` 와 다름 | 삭제 후 올바른 암호 재입력 |
| `암호는 맞습니다 ✓ 그러나 서버의 GitHub 토큰이 만료/권한부족입니다` | `GH_TOKEN` 만료 | 아래 순서로 갱신 |

**갱신 방법**

1. [새 Fine-grained 토큰 발급](https://github.com/settings/personal-access-tokens/new) —
   Repository: `Henryshin/flight-deals`, 권한: **Contents (Read and write)** +
   **Actions (Read and write)**
2. Worker → **Settings → Variables and Secrets** → `GH_TOKEN` 값을 새 토큰으로 교체 → **Deploy**
3. 대시보드에서 `암호 확인` 을 다시 눌러 `GitHub 토큰도 정상` 이 뜨는지 확인

> Worker 는 GitHub 의 401/403 을 그대로 전달하지 않고 **502 + 안내 문구**로 바꿔서
> 돌려줍니다. 그대로 넘기면 프런트가 "공유 암호가 틀렸다"로 오해해, 맞는 암호를 계속
> 다시 입력하게 만들기 때문입니다. 이 동작을 쓰려면 `worker.js` 최신본이 배포돼 있어야
> 합니다(위 2단계에서 코드도 함께 다시 붙여넣으면 됩니다).

## (선택) 동시접속자 카운터

대시보드 상단에 "지금 몇 명 접속 중"을 표시하려면 KV 네임스페이스를 하나 만들어
이 Worker에 연결하세요. (약 2분, 코딩 없음) 누적 조회수는 이 Worker와 무관하게
`docs/index.html`에 붙은 별도 위젯(abacus)이 표시합니다.

1. Cloudflare 대시보드 → **Storage & Databases → KV** → **Create namespace**.
   이름은 예: `flight-counter` → **Add**.
2. Worker(`flight-register`) → **Settings → Bindings** → **Add binding → KV Namespace**.
   - Variable name: **`COUNTER_KV`** (철자 그대로, 코드가 이 이름으로 찾습니다)
   - KV namespace: 방금 만든 `flight-counter` 선택 → **Deploy**.

바인딩이 없으면 카운터 위젯은 그냥 숨겨질 뿐, 등록/삭제 등 다른 기능에는 영향 없습니다.
동시접속자 집계는 등록 프록시와 무관하게 방문자 전원에게 열려 있고(공유 암호 불필요),
"최근 2분 안에 하트비트를 보낸 탭 수"의 근사치입니다.

채팅 제목표시줄 우측상단의 "👥 N" 탭을 누르면 지금 접속 중인 사람들의 이름(닉네임) 목록이
펼쳐집니다. 하트비트 요청에 채팅과 같은 clientId를 함께 실어 보내 이름을 붙이는 방식이라
추가 KV 쓰기는 없고(하트비트 한 번에 이름까지 같이 저장), 커스텀 닉네임을 설정한 사람은
그 이름 그대로 보입니다.

## (선택) 실시간 채팅

대시보드의 필터 옆 "실시간 채팅" 패널은 위에서 만든 **같은 `COUNTER_KV` 바인딩**을 그대로 씁니다.
즉 동시접속자 카운터를 이미 설정했다면 추가 작업 없이 바로 동작하고, 설정하지 않았다면
채팅 섹션 전체가 숨겨집니다.

- 로그인·닉네임 입력 없이 누구나 짧은 메시지(최대 300자)를 남길 수 있고, 최근 200개까지
  KV에 보관되며 화면에는 최근 50개가 보입니다.
- 채팅 기록은 계속 남지 않고 **KST(한국시간) 자정 기준으로 매일 리셋**됩니다. 어제까지의
  메시지는 자정이 지나면 더 이상 보이지 않고, 새 하루의 메시지부터 다시 쌓입니다.
- 닉네임은 기본적으로 **접속 IP를 해시해 서버가 자동으로 배정**합니다
  ("형용사 2~3개 + 캐릭터 + 직급" 조합, 예: "현명한 용감한 루팡 대리"). 같은 IP는
  항상 같은 닉네임을 받고 계속 유지됩니다.
- 채팅 패널의 "✎ 닉네임 바꾸기"로 원하는 닉네임을 직접 지정할 수도 있습니다(최대 16자).
  지정한 닉네임은 브라우저(clientId) 기준으로 KV에 180일간 보관되며, 빈 값으로 저장하면
  다시 자동 배정 닉네임으로 돌아갑니다. 사칭 방지를 위해 5초에 한 번만 변경할 수 있고,
  이미 보낸 과거 메시지의 이름은 소급 변경되지 않습니다.
- "관리자"는 예약된 닉네임이라 아무나 가져갈 수 없습니다. 설정하려면 아래 `CHAT_ADMIN_PASS`
  시크릿과 일치하는 암호를 입력해야 하며, 이 시크릿을 등록하지 않으면 "관리자" 닉네임은
  누구도(사이트 관리자 본인 포함) 사용할 수 없습니다. Worker의 **Settings → Variables and
  Secrets**에서 `CHAT_ADMIN_PASS`(Secret)에 본인만 아는 암호를 넣어두세요 —
  친구들에게 공유하는 `SHARE_PASS`와는 다른, 나만 아는 값이어야 합니다.
- 닉네임이 "관리자"인 사람에게는 채팅 말풍선 우측상단에 ✕ 버튼이 보이고, 눌러서 그
  메시지 하나를 지울 수 있습니다(처음 누를 때만 `CHAT_ADMIN_PASS`를 물어보고, 그 뒤로는
  같은 탭에서 새로고침 전까지 다시 묻지 않습니다). 실제 삭제는 서버가 `CHAT_ADMIN_PASS`를
  다시 검증하므로, 닉네임만으로는(예: 옛날에 설정해 둔 이름) 삭제할 수 없습니다.
- 말풍선의 좌/우 정렬(내 메시지 = 오른쪽)은 clientId 기준으로 판단하므로, 닉네임을
  바꿔도 예전에 보낸 내 메시지가 계속 오른쪽에 표시됩니다.
- 진짜 실시간(WebSocket)이 아니라 **5초 간격 폴링**이라 다른 방문자의 메시지가 몇 초
  늦게 보일 수 있습니다. KV는 최종 일관성이라 여러 명이 동시에 보내면 아주 드물게
  메시지가 유실될 수 있습니다 — 소규모(친구·소모임) 용도로 만든 절충입니다.
- 도배 방지를 위해 같은 브라우저(clientId)는 3초에 한 번만 전송할 수 있습니다.

## (선택) 여행지 제안 투표

채팅 패널 위쪽 "🗳️ 다음에 가고 싶은 곳"도 같은 `COUNTER_KV` 바인딩을 그대로 씁니다.
"+ 제안"으로 여행지 이름(최대 40자)을 자유롭게 적어 올리면 칩으로 표시되고, 다른
방문자가 칩을 눌러 투표(다시 누르면 취소)할 수 있습니다. 이미 있는 제안과 같은
이름(대소문자·공백 무시)을 또 올리면 새로 만들지 않고 그 제안에 투표만 더합니다.

- 실제 노선 등록과는 무관한 "인기 투표" 게시판일 뿐이라, 득표 결과를 보고 운영자가
  `data/routes.json`에 수동으로 반영합니다.
- 채팅과 달리 매일 리셋되지 않고 계속 누적되며, 최대 60개까지 보관합니다(초과 시
  최저 득표 제안부터 제거).
- 도배 방지로 같은 브라우저는 5초에 한 번만 새 제안을 올릴 수 있고, 투표(클릭)는
  1초에 한 번으로 제한됩니다.
